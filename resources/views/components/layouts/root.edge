<!DOCTYPE html>
<html lang="en" hx-boost="true" hx-headers='{"x-csrf-token":"{{ csrfToken }}"}'>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  @entryPointStyles('app')
  @entryPointScripts('app')
</head>
<body class="{{ className || ''}}">
  {{{ await $slots.main() }}}
</body>
</html>
